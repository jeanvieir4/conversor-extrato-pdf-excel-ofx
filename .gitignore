# Extratos e resultados gerados - dados financeiros de clientes, nunca versionar
*.pdf
*.PDF
*.xlsx
*.xlsm
*.ofx
~$*

# Executavel compilado e pacote de distribuicao (50MB+, vai pro GitHub Releases, nao pro git)
*.exe
*.zip

# Python
__pycache__/
*.pyc

# Saida do PyInstaller
build/
dist/

# Pasta do programa empacotado (modo --onedir: exe + DLLs + _internal),
# copiada manualmente pra Para_Equipe antes de zipar - nao e codigo-fonte
Para_Equipe/Conversor_Extratos/
